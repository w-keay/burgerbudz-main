// Import the 'Navbar' component from the specified path
import Navbar from './Navbar';

// Import 'TypingText' and 'TitleText' components from the specified path
import { TypingText, TitleText } from './CustomTexts';

// Import the 'ExploreCard' component from the specified path
import ExploreCard from './ExploreCard';

// Import the 'StartSteps' component from the specified path
import StartSteps from './StartSteps';

// Import the 'NewFeatures' component from the specified path
import NewFeatures from './NewFeatures';

// Import the 'InsightCard' component from the specified path
import InsightCard from './InsightCard';

// Import the 'Footer' component from the specified path
import Footer from './Footer';

// Export the imported components as named exports from this module
export {
  Navbar,
  TitleText,
  TypingText,
  StartSteps,
  NewFeatures,
  ExploreCard,
  InsightCard,
  Footer,
};
